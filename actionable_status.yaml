# Current actionable implementation status for the Rust codebase.
#
# Scope:
# - Keep only items that still need code changes.
# - Do not mirror implementation_state.yaml or list completed work.
# - Remove items as soon as they are fixed and covered by tests, then update implementation_state.yaml with the completed behavior.
# - Prefer concrete file/line evidence over broad percentages.
#
# Structure:
# items:
#   - id: stable-short-id
#     area: crate-or-module
#     priority: low|medium|high
#     status: open
#     summary: "One concrete implementation gap."
#     evidence:
#       - path: "relative/path.rs"
#         note: "Specific failing behavior, missing coverage, or source evidence."
#     impact: "Why this still matters."
#     next_steps:
#       - "Smallest useful implementation or investigation step."
#     refuted:
#       - "A claim this item used to make that measurement has since killed."
#
# `refuted` is optional and exists so an item that survives a measurement does
# not silently re-acquire the reading that measurement disproved. Put a claim
# there when the item stays open but its evidence, cause or size has changed;
# the numbers behind it belong in PERFORMANCE.md, and only the verdict here.
#
# An empty items list means there are currently no known actionable implementation
# gaps. Completed behavior and policy decisions belong in implementation_state.yaml.
# Malformed-input/resource-hardening status is tracked separately in
# hardening_status.yaml; keep only concrete implementation actions here.

version: 1
last_reviewed: "2026-09-01"

items:
  - id: pgo-two-pass-build
    area: web
    priority: low
    status: open
    summary: "Profile-guided optimisation is documented and measured at 9-12%, and the only build here it could be applied to is the WASM one, where neither figure holds."
    evidence:
      - path: "PGO.md"
        note: "The two-pass build, the training-corpus guidance, and why a source library cannot enable PGO for anyone: it is a flag on whoever compiles the final binary, and nothing is done inside the crates."
      - path: "web/Cargo.toml"
        note: "The seven *-wasm modules are the only binaries this repository ships, and they consume draco-core, draco-io and draco-gltf directly. Release there is a size build -- panic = abort, strip, opt-level \"z\" for everything but draco-core -- where the 9-12% measured at -Copt-level=3 has neither its baseline nor its whole mechanism, cold-path outlining being part of what \"z\" already does."
    impact: "Not a code change in any crate, and not draco-io's: a downstream consumer's native binary would gain the documented amount, and giving it to them is what PGO.md already does. What is undone is only the in-repo half, which is the WASM half -- and the cheaper lever there was taken first: raising draco-core's own opt-level to 2 is worth 1.74-1.78x, more than PGO claims natively, for a manifest change and no profile to collect."
    next_steps:
      - "If it is taken up at all, take it on web: an instrumented wasm build, a profile collected in a browser or a WASI runner, and a re-measurement on that target against the current profile, not against \"z\" -- the codec has already moved off it."
      - "Decide the size budget first, and note that the opt-level round already spent 24.7% of drc-wasm's gzip. web/wasm-sizes.md records what each module weighs."
      - "Pick the training corpus before the machinery: meshes and point clouds, the attribute types actually used, a spread of speeds. A profile trained on one shape can lose on another."
    refuted:
      - "That this belongs to draco-io. PGO is a flag on the final binary, and there is no native binary here to set it on -- the only main.rs files are web/build-tool and web/dev-server, neither a codec, and the benchmark harnesses are dev tooling."

  - id: decoder-buffer-reuse-across-decodes
    area: draco-core
    priority: low
    status: open
    summary: "A decode re-allocates every attribute's DataBuffer even when it is handed a mesh whose buffers are the right size, because clearing the mesh drops them with it."
    evidence:
      - path: "PERFORMANCE.md"
        note: "\"The Reuse Two Items Proposed, Which Was A Correctness Bug\". Reusing the mesh saves 2-3 allocations and 0.22 MB of 1.57 on an 18k-face decode -- the faces Vec -- and the values are re-allocated either way. Seven interleaved rounds: one payload of six at 0.914x with 7/7 signs, everything else inside its spread, both round-robin mixes included."
      - path: "crates/draco-core/src/mesh.rs"
        note: "`Mesh::clear` keeps the face list's capacity and the attribute list's spine, and drops each PointAttribute -- and with it the DataBuffer holding the values, which is where the bytes are."
    impact: "Aimed at the repeated-decode caller -- a viewer or a server decoding many assets -- not at one-shot decode. Measured on that caller rather than assumed: it is worth less than the spread on five of six payloads and on a round-robin over four differently-sized streams, so this is a lead and not a pending win."
    next_steps:
      - "Do not add a reuse entry point: the reuse it would expose is what `decode` already does, and it was measured. Any work here is inside the decoder."
      - "The one shape that could still pay: reuse an existing PointAttribute whose layout the new stream matches, instead of dropping it in `clear`. Price it against the risk first -- it can leave a previous decode's values wherever the new one does not overwrite, which is the bug just fixed, and no count catches that class."
      - "Whatever is tried, measure it on a round-robin of differently-sized streams. One payload repeated hands the reused mesh buffers already exactly the right size, which is the best case and not the case."
    refuted:
      - "That reuse is expressible today and merely unexposed. It was expressible and it was wrong: `decode` never cleared the mesh it was given, so a second decode left it carrying both streams' attributes -- six after two decodes of a position/normal/texcoord stream, sixty after twenty -- with the point and face counts right the whole way. Fixed, and pinned by `a_reused_decoder_and_mesh_decode_what_fresh_ones_do`."
      - "That reuse is worth 21-28% on large meshes. That was the first reading and it was the bug: the reused arm never freed the previous decode. Against the fixed decoder the same payload reads 1.021x."
      - "That the cost is per-call setup. Decode allocates 42 times for 8 faces and 45 for 19,602 -- a per-call floor, as encode has -- but the floor is not what reuse reaches, and it is not where the decode's time is on anything but a tiny mesh."


  - id: encode-setup-cost-on-small-meshes
    area: draco-core
    priority: medium
    status: open
    summary: "An encode allocates the same ~85 times whether the mesh has 8 faces or 19,602, so about 85% of a tiny encode is per-call cost that no per-face work can reach."
    evidence:
      - path: "PERFORMANCE.md"
        note: "\"The Small-Mesh Encode Cost, Reproduced Against Its Own Hypothesis\". Allocations per encode at speed 5: 85 on an 8-face mesh, 86 on a 19,602-face one. An 8-face encode costs 4.9 us against the 0.093 us/face the large mesh pays, so 0.75 us of it is its faces."
      - path: "crates/draco-core/src/mesh_encoder.rs"
        note: "`encode` opens with `self.options = options.clone()`, which copies two HashMaps of String keys -- 6 allocations for keys that are string literals at every call site here. The rest of the fixed count is edgebreaker-encoder setup (~23 of the first 64 sampled), encode_edgebreaker_attributes_split (9) and CornerTable::init (11)."
    impact: "Encode only; decode has its own item. It matters for the caller encoding many small meshes -- a glTF with hundreds of tiny primitives pays this per primitive -- and not at all for one large mesh."
    next_steps:
      - "Name the whole fixed count, not the first 64: encode_matrix's SAMPLE_LIMIT is not env-controlled, so raise it alongside SAMPLE_ALLOC_MIN/MAX before sampling a payload that allocates more than that."
      - "Take the cheapest block first and measure it alone: the options clone is 6 allocations of pure bookkeeping, and interning the keys keeps EncoderOptions' fields private and the API additive."
      - "Measure on a mesh small enough for the floor to show -- an 18k-face payload dilutes a 4 us effect to 0.2%, and the seeded sweep is the control the change must not cost anything on."
    refuted:
      - "That the 2.0 series (7a277ff..f18f3e1) left small-mesh encode 3-13% slower. Measured payload by payload with the release profile matched across revisions: 3.6% at 8 faces at speed 5 (7/7 rounds) and nothing resolvable at 12 faces or above, with the series a 14-15% win at speed 1 from 170 faces up."
      - "That the memoisation commits are the cause. The series changed the allocation count by zero on every speed-5 cell measured; the entropy cache grows lazily from empty and the geometric-normal caches need a normal attribute the 8-face payload does not have. A face-count gate would have had no setup to skip."
      - "That any of it stands today. 4353e7a is faster than 7a277ff on every payload and speed measured, 0/7 signs throughout -- 0.85x on the 8-face mesh, 0.68-0.72x on the 18k one."
